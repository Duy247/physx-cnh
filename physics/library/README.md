# New PDF storage

All new contributor PDFs are stored under a catalog-specific directory here. Do not move historical PDFs into this directory because their existing public URLs must remain stable.

Use `php tools/catalog.php add ...` from the repository root to create the directory, copy the PDF and register it safely.
